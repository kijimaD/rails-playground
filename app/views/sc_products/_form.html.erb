<%= form_with(model: sc_product, local: true) do |form| %>
  <% if sc_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sc_product.errors.count, "error") %> prohibited this sc_product from being saved:</h2>

      <ul>
        <% sc_product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
